import { JsonconvertPipe } from './jsonconvert.pipe';

describe('JsonconvertPipe', () => {
  it('create an instance', () => {
    const pipe = new JsonconvertPipe();
    expect(pipe).toBeTruthy();
  });
});
